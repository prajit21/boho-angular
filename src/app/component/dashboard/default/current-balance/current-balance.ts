import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-current-balance',
  templateUrl: './current-balance.html',
  styleUrls: ['./current-balance.scss'],
  imports: [RouterLink],
})
export class CurrentBalance {}
